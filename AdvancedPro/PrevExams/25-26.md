## Question 1 (10 Marks)
### Choose the Correct Answer:

1.  **When returning a redirect prefix (e.g., `return "redirect:/target";`) from a Spring MVC controller, which of the following occurs?** [cite: 669]
   *  A. The request attributes are preserved across the redirect. [cite: 670]
   *  B. The client's browser URL remains unchanged. [cite: 671]
   *  C. The client's browser URL changes to the new resource. [cite: 672]
   *  D. A and B [cite: 673]

2.  **Within an HTML form, which `<input>` attribute determines the name associated with the submitted value?** [cite: 674]
   *  A. submit [cite: 675]
   *  B. param [cite: 676]
   *  C. id [cite: 679]
   *  D. name [cite: 680]

3.  **The Java Persistence API (JPA) is considered part of the Java ________ platform.** [cite: 677, 681]
   * A. SE [cite: 677]
   *  B. EE [cite: 678]
   *  C. ME [cite: 681]
   * D. TV [cite: 682]

4. **In Spring Data JPA, the repository interfaces can handle the following database operations automatically through query methods or `@Query` annotations:** [cite: 683, 684]
   *  A. SELECT [cite: 685]
   *  B. UPDATE [cite: 686]
   * C. DELETE [cite: 687]
   *  D. All of them [cite: 688]

5.  **Which Thymeleaf attribute is used to include or insert a template fragment?** [cite: 689]
   * A. `th:insert` [cite: 691]
   *  B. `th:import` [cite: 693]
   *  C. `th:include` [cite: 693]
   * D. `th:replace` [cite: 693]

6. **In Spring Boot, how do we define a bean scope so that its lifecycle is restricted to a single HTTP request?** [cite: 694, 697]
   *  A. `@Scope("session")` [cite: 695]
   * B. `@Scope("request")` [cite: 698]
   * C. `@Scope("application")` [cite: 696]
   * D. `@Scope("singleton")` [cite: 699]

7.  **Which of the following methods causes a thread to pause execution for a specified time period?** [cite: 700]
   * A. sleep () [cite: 701]
   *  B. join () [cite: 702]
   *  C. wait () [cite: 703]
   * D. None of them [cite: 704]

8. **What will be the output when two threads simultaneously call the following method?** [cite: 705]
   ```java
   public synchronized void increment () {
       x++;
       y++;
   }
   ```

## Question 1 (Continued)

9.  **What is the default scope of a Spring Bean in Spring Boot?** [cite: 59]
   *  A. session [cite: 59]
   *  B. prototype [cite: 60]
   *  C. request [cite: 61]
   *  D. singleton [cite: 65]

10.  **In Spring Boot's multi-layered MVC architecture, which component/layer is responsible for business logic?** [cite: 62]
     *  A. Model / Service [cite: 62]
     *  B. View / Thymeleaf Template [cite: 62]
     *  C. Controller [cite: 63]
     *  D. All of them [cite: 66]

---

## Question 2 (12 Marks)
###  Give brief answers to the following questions: [cite: 64, 67]
1.  Three advantages of using Spring Boot/Thymeleaf over Java Servlets/JSP. [cite: 68]
2.  Three differences between a JavaBean and a Spring Bean. [cite: 69]
3.  How does synchronization prevent race conditions in multithreading? [cite: 70]
4.  When using multithreading, why should we override the `run()` method from Runnable, but we do not call it explicitly? [cite: 71, 72]

---

##  Question 3 (10 Marks) [cite: 73]

1.  **Given the following Thymeleaf template snippets and the requirement to bind student registration data, write the equivalent Spring Boot Controller class and the Student class:** [cite: 74, 75]

   ```html
   <!-- student-form.html -->
   <form th:action="@{/register}" th:object="${student}" method="post">
       <input type="text" th:field="*{name}" />
       <input type="number" th:field="*{level}" />
       <button type="submit">Submit</button>
   </form>

   <!-- result.html -->
   <p th:text="'Number of students in level 1 is ' + ${firstCount}"></p>
   ```

2. **write program  that has array of size 1000 filled with integers . program should use 2 threads , first calc mx elemnt and other calc mim element in it . At the end . the program should calculate and display result d = mx -min**

## Question 4 (8 Marks)

Given a database table `Players` mapped to an entity `Player`, write the Spring Data JPA repository interface and the Service/Controller code snippet to:
1. Insert a new player into the table using the `save()` method (using any fake data).
2. Delete all players from the table who play in team `"Angola"` (declare/use a custom delete query method).
3. Display at the console the data of all players who play in team `"Egypt"` and their position is either (`"GK"` or `"RW"`) (declare/use a custom query method).